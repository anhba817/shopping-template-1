const styles = (theme) => ({
  menuContainer: {
    padding: 10,
  },
  iconActive: {
    color: theme.palette.primary.main,
  },
  menuButton: {
    color: theme.palette.primary.main,
    textTransform: 'none',
  },
  buttonText: {
    fontWeight: 700,
    fontSize: 16,
  }
});

export default styles;
