const styles = (theme) => ({
  radioRoot: {
    display: 'flex',
    flexWrap: 'wrap',
  },
  radioCarousel: {
    padding: '0 42px',
    marginLeft: -20,
    marginRight: -20,
  }
});

export default styles;
